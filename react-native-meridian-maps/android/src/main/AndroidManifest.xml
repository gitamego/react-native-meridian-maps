<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <!--
    Your app’s debug Manifest.
    The ‘package’ here defines your app’s namespace.
  -->

  <application
      android:name="meridianmaps.example.MainApplication"
      tools:replace="android:name"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

      <!-- ReactActivity entry point -->
      <activity
          android:name="com.facebook.react.ReactActivity"
          android:exported="true">
          <intent-filter>
              <action android:name="android.intent.action.MAIN"/>
              <category android:name="android.intent.category.LAUNCHER"/>
          </intent-filter>
      </activity>

      <!-- Any other app activities… -->
      <activity android:name=".MainActivity"/>

  </application>
</manifest>
